# Satyam CRM Desktop

This desktop wrapper opens the live CRM web app in a desktop window.

## Run locally

```bash
npm install
npm start
```

## Build Windows portable executable

```bash
npm run dist
```

This creates a portable .exe in the `dist` folder.
